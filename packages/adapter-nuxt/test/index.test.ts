import { test, expect } from "vitest";

import * as lib from "../src/plugin";

// TODO: Dummy test, meant to be removed when real tests come in
test("exports something", () => {
	expect(lib).toBeTruthy();
});
